"""Infrastructure layer: implementations of domain ports (Postgres, Neo4j, git,
filesystem, LLM clients, ...). Empty in Phase 1 — populated starting Phase 2
(Repository Import) once there is a domain port for it to implement.
"""
